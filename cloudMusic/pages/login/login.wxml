<view class="container">
  <view class="wrapper">
    <view class="left-top-sign">LOGIN</view>
    <view class="welcome">
      欢迎回来！
    </view>
    <view class="input-content">
      <view class="input-item">
        <text class="tit">手机号码</text>
        <input  type="text" placeholder="请输入手机号码" data-type="phone" id="phone" bindinput="handleInput"/> <!-- bindinput在表单上时事触发，change只有当失去焦点的时候触发 -->
      </view> 
      <view class="input-item">
        <text class="tit">密码</text>
        <input type="password" placeholder="请输入密码" data-type="password" id="password" bindinput="handleInput"/> <!-- data-(key)="(value)" 传多个值时使用 ； id是唯一的，只能传一个值标识 -->
      </view>
    </view>
    <button class="confirm-btn" bindtap="login">登录</button>
    <view class="forget-section">
      忘记密码?
    </view>
  </view>
  <view class="register-section">
    还没有账号?
    <text >马上注册</text>
  </view>
</view>
